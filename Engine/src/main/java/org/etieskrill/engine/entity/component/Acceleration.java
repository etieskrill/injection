package org.etieskrill.engine.entity.component;

import org.joml.Vector3fc;

public class Acceleration extends DirectionalForceComponent {
    public Acceleration(Vector3fc force) {
        super(force);
    }
}
